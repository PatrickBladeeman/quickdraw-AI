# quickdraw-AI — Current Authorized Task

Last updated: 2026-09-07

This file is the canonical answer to **what work is authorized right now**.
It excludes completed-task history and speculative later work.

## Task

### Second implementation consolidation

Reduce independently maintained executable mechanisms and test construction
on top of the existing acceptance/update-gate consolidation. Current results
and the unchanged research frontier belong in [`STATE.md`](STATE.md).

## Implementation boundary

- Share trajectory orchestration, repeated contract/prefix algorithms, and
  milestone fixture/plumbing tests while retaining historical entry paths.
- Share matching Unity PlayMode lookup/reflection behavior in the existing
  test assembly; keep fixture setup and behavioral assertions local.
- Reuse the process launcher for R3P and consolidate identical raw-file/runtime
  utilities with a clean dependency direction for checkpoint persistence.
- Evaluate small Unity build helpers and LLAPI collection mechanics separately.
  Build helper extraction is in scope; collection-loop extraction requires a
  small design and focused protection of exact event order and boundaries.
- Keep protocol-specific R3P/R3Q control flow and distinct research assertions.

## Acceptance criteria

- Demonstrate material net reduction including every added module, test, and
  helper, measured against the actual task-start tree.
- Map merged tests to retained claims; verify real composed validators,
  historical CLI modes, failures, fresh outputs, and exact comparison rules.
- Run focused and complete trainer checks, affected Unity tests, and narrow
  real build verification when build orchestration changes.
- Reproduce unchanged acceptance boundaries in fresh ignored directories as
  needed, including R3P process launch and affected R3Q saver/restorer behavior.
- Reconcile the complete frozen hash manifest and affected path/SHA bindings;
  review the final diff through the configured read-only Kilo workflow.

On 2026-09-07 the user authorized completion without restoring the overwritten
`Research_Basic_timers.json` profiling log. Retain its original hash and the
observed mismatch in the audit; this exception does not change any scientific
contract, accepted evidence, or other preservation requirement.

## Constraints

Do not select or execute an action after update 5, collect transition 10,017,
run optimizer update 6, synchronize the target network, resume the Unity
process, start extended training, export a model, or make an effectiveness
claim. Preserve RNG consumption, update timing, observations/actions/rewards,
checkpoint encoding, failure ordering, and frozen contracts, schemas, design,
curated evidence, and accepted raw artifacts. Broad production-core refactors
and gameplay changes are outside scope. Do not commit, push, open a PR, deploy,
or publish without explicit authorization.
